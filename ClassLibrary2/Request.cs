// For some reason the other team decided to have identical namespaces in different nuggets, which I consume.
// Thus, we have have duplicate unique class names.
namespace ClassLibrary1  
{
    public class Request
    {
    }
}